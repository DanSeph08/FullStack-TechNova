import { SxProps } from "@mui/system";
import { ReactNode } from "react";

interface SvgIconosFilledProps {
  children?: ReactNode;
  sx?: SxProps;
  color?: string;
}

export type { SvgIconosFilledProps };